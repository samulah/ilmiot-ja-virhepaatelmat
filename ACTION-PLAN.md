# SEO Action Plan — ilmiöt.fi

Generated: 2026-06-19 | Target domain: https://www.ilmiöt.fi
Companion to `FULL-AUDIT-REPORT.md`

---

## 🔴 CRITICAL — do before any deploy

> ✅ **C1 and C2 DONE (2026-06-19).** Domain chosen: **www.ilmiöt.fi**. 895 site URLs rewritten across HTML/sitemap/robots/llms; WebSite schema retargeted to ilmiöt.fi; publisher Organization kept as "Kendom". CSP `script-src` now includes `'unsafe-inline'`. **C3 (deploy) and H1 (host redirect) remain.**

### C1. ✅ DONE — Canonical domain = www.ilmiöt.fi, all references rewritten
The whole build canonicalizes to `kendom.fi/ilmiöt/`, but you're treating `ilmiöt.fi` as the site. **Pick one home domain.** Recommended: **`www.ilmiöt.fi`** — it's an exact-match, brandable domain ("ilmiöt.fi" = "phenomena.fi") and cleaner than a subdirectory on kendom.fi.

Once decided, update every reference (one scripted search-replace):
- All `*.html`: `<link rel="canonical">`, `og:url`, `og:image`, `twitter:image`, schema `@id`/`url`.
- `sitemap.xml`: all `<loc>`.
- `robots.txt`: the `Sitemap:` line.
- `llms.txt`: all URLs.

```bash
# from repo root — review the diff before committing
grep -rl 'kendom.fi/ilmiöt' . --include='*.html' --include='*.txt' --include='*.xml' \
  | xargs sed -i 's#https://kendom.fi/ilmiöt#https://www.ilmiöt.fi#g'
```
If kendom.fi/ilmiöt/ must also stay live, set up a site-wide 301 from kendom.fi/ilmiöt/* → ilmiöt.fi/* so authority consolidates on one domain.

### C2. ✅ DONE — CSP now allows inline scripts
`.htaccess` `script-src` is now `'self' 'unsafe-inline' cdn.jsdelivr.net`, so the 196 inline scripts (Mermaid init, Chart.js, random.html redirect) won't be blocked.
(Cleaner long-term: move inline scripts to external files or add per-script nonces, then drop `'unsafe-inline'`.) **Still verify Mermaid/Chart render and random.html redirects on a staging copy before going live.**

### C3. Deploy the build
Live site is still the pre-SEO version (last-modified 2026-06-16 13:23). Push the repo's HTML, `og/`, `sitemap.xml`, `robots.txt`, `llms.txt`, and the corrected `.htaccess`. Re-check the live homepage afterwards for `<link rel="canonical">`, meta description, and the security headers.

---

## 🟠 HIGH

### H1. ✅ DONE — Host canonicalization (www + https)
`.htaccess` now 301-redirects every non-canonical host/scheme to `https://www.xn--ilmit-mua.fi/` (the punycode of www.ilmiöt.fi). Consolidates non-www and any legacy host onto one origin. **Verify after deploy** that LiteSpeed's existing HTTP→HTTPS rule doesn't create a redirect loop.

### H2. ⏳ DEFERRED (per request) — "Liittyvät ilmiöt" (related phenomena) blocks
Append a 3–6 link related-phenomena section to each of the 68 article pages (group by the existing 8 categories, plus a few cross-category links). Biggest remaining on-page win: improves crawl depth, internal PageRank flow, topical clustering, and AI co-citation. Generatable from the category map in `llms.txt` / `MEMORY.md`. **Still open.**

### H3. ✅ DONE — Meta descriptions rewritten
All 68 phenomenon descriptions regenerated by `scripts/seo_eeat.py` into clean, full-sentence Finnish (≤158 chars rendered), no mid-word cuts, no "X: X" repetition. Synced across `meta description`, `og:description`, `twitter:description`, and Article/DefinedTerm schema.

### H4. ✅ DONE — E-E-A-T signals
- `author` = **Ilmiömies** (Person) + `datePublished` (2026-06-16) + `dateModified` (2026-06-19) added to every Article JSON-LD. ⚠️ Dates are placeholders — adjust if you have real ones.
- Visible byline ("Kirjoittanut Ilmiömies · Päivitetty 19.6.2026") after each H1.
- New **`tietoa.html`** About page (who writes this, purpose, method, usage) with AboutPage + Person schema; linked from the footer on every page and from `llms.txt`.
- Publisher Organization changed from "Kendom" → "Ilmiöitä"; **all Kendom references removed sitewide.**

---

## 🟡 MEDIUM

- **M1.** ✅ DONE — `random.html` now has `noindex, follow` + self-canonical.
- **M2.** ✅ PARTIAL — the 3 genuinely short phenomenon titles lengthened with descriptive modifiers. The ~27 titles over 60 chars were left (SERP truncation is minor and rewriting them well is editorial — revisit if desired).
- **M3.** ❎ NON-ISSUE — on closer inspection `CollectionPage` only appears as a valid `isPartOf` reference on article pages, not as the page's own type. No change needed.
- **M4.** ⏳ OPEN (needs your accounts) — Connect Google Search Console + Bing Webmaster for www.ilmiöt.fi; submit `sitemap.xml`; capture CrUX field data for real CWV.
- **M5.** ⏳ OPEN — Monitor CLS/INP on diagram-heavy pages after deploy (reserve space for Mermaid containers to prevent layout shift).

---

## 🟢 LOW — backlog

- **L1.** Convert OG images PNG → WebP/JPEG (~50% smaller).
- **L2.** Self-host fonts to remove the third-party Google Fonts request.
- **L3.** Add `dateModified` automation tied to file mtime in the sitemap and schema.
- **L4.** Consider a lightweight category index/hub page per category to strengthen the hub-and-spoke structure.

---

## What's left

1. **C3 — Deploy.** Everything above is local. Push the repo (HTML, `tietoa.html`, `og/`, `sitemap.xml`, `robots.txt`, `llms.txt`, `.htaccess`) and re-verify the live homepage shows the canonical, descriptions, security headers, and that Mermaid/Chart/random still work under the CSP.
2. **H2 — related-phenomena links** (biggest deferred win).
3. **M4/M5** — Search Console + post-deploy CWV monitoring.

**Projected score after deploy:** ~83–85/100 (from 28 live today).
